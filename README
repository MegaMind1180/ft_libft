*This project has been created as part of the 42 curriculum by wmezgoli.*

# libft

## Description

libft is a custom C library that reimplements a selection of standard C library functions, as well as additional utility functions commonly used in C programming. The goal of the project is to build a solid foundation of reusable functions that can be used in future 42 projects, while deepening the understanding of low-level memory management, string manipulation, and linked list operations.

The library is divided into three parts:

- **Part 1** — Reimplementations of standard libc functions (`ft_strlen`, `ft_memcpy`, `ft_strchr`, `ft_atoi`, etc.)
- **Part 2** — Additional utility functions not found in libc (`ft_substr`, `ft_strjoin`, `ft_split`, `ft_itoa`, etc.)
- **Part 3** — Linked list functions (`ft_lstnew`, `ft_lstadd_back`, `ft_lstmap`, etc.)

## Instructions

### Compilation

Clone the repository and compile the library using the provided Makefile:

```bash
git clone <your-repo-url>
cd ft_libft
make
```
### Using the library in your project

Include the header and link the library when compiling:

```bash
gcc your_file.c -L. -lft -I. -o your_program
```

### Cleaning up

```bash
make clean   # removes object files
make fclean  # removes object files and libft.a
make re      # fclean + recompile
```

## Resources

- [C Standard Library Reference — cppreference.com](https://en.cppreference.com/w/c)
- [Valgrind User Manual](https://valgrind.org/docs/manual/manual.html) — used to detect memory leaks and invalid memory accesses
- [42 Tripouille libft tester](https://github.com/Tripouille/libftTester) — test suite used to validate the implementations
- [Understanding size_t and unsigned types in C](https://www.embedded.com/why-size_t-matters/)

